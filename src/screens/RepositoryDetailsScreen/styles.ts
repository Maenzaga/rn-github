import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    padding: 16,
    alignContent: 'center',
    backgroundColor: 'white',
  },

  title: {
    fontWeight: 'bold',
    fontSize: 20,
    marginBottom: 8,
  },
});
